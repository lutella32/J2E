{
    "<br>liste</br>": [
        {% for robot in liste %}

        {
            "id": "{{ robot.identifiant }}",
            "nom robot": "{{ robot.Nom }}",
            "marque robot": "{{ robot.Marque.nomMarque }}",
            "id de l'Analyse": "{{ robot.Analyse.idAnalyse }}",
            "nom de l'analyse": "{{ robot.Analyse.nomAnalyse }}",
            "nom patient": "{{ robot.Analyse.nomPatient }}",
            "resultat Patient": "{{ robot.Analyse.resultat }}",
             "covid": "{{ robot.Covid.version }}"
        }
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}
{
    "<br>liste</br>": [
        {% for robot in liste %}

        {
            "<br>id": "{{ robot.identifiant }}",
            "nomrobot": "{{ robot.Nom }}",
            "id de l'Analyse": "{{ robot.Analyse.idAnalyse }}",
            "nom patient": "{{ robot.Analyse.nomPatient }}",
            "resultat Patient": "{{ robot.Analyse.resultat }}",

            "marque": "{{ robot.Marque.nomMarque }}",

            "covid": "{{ robot.Covid.version }}</br>"
        }
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}